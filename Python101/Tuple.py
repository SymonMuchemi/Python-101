# We use parenthesis to represent tuples
# Tuples are immutable

my_tuple = (1, 2, 3, 4)
print(my_tuple.count(4))

# Unpacking

coordinates = (1, 2, 3)

x, y, z = coordinates # Instance of unpacking
print(x, y, z)